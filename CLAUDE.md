# Wrong repo

This is a deploy-only repo containing built artifacts. You cannot make meaningful changes here.

The source code lives in the `radiatormouthwash/dbt` repo under `freedbt/`. Tell the user they likely meant to use that repo instead.
